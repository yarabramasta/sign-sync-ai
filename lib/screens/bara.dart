import 'package:flutter/material.dart';

class BaraScreen extends StatelessWidget {
  const BaraScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
